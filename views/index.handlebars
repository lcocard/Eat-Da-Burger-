  
<div class="row">
  <div class="col-xs-2 col-sm-3 col-md-4 uneaten">
      <h3>EAT DA BURGER!</h3>
    <form action="/{{this.id}}?_method=DELETE" method="POST">
      {{#each burgers}}
        
          <p>{{this.id}}. {{this.burger_name}}  <button type="submit" class="btn btn-submit btn-xs" id="devour">Devour it!</button></p>        
        
      {{/each}}
    </form>
  </div>
  <div class="col-xs-8 col-sm-6 col-md-4" id="sign">
  		<img src="/assets/hamburgers.jpg">
  		<h1>Eat-Da-Burger!</h1></div>
  <div class="col-md-4 col-md-4 col-md-4"></div>
</div>

<div class="row">
  <div class="col-md-4 col-md-4 col-md-4">   
  </div>
  <div class="col-md-4 col-md-4 col-md-4" id="addBurger">
  	<form method="POST" action="/create">
  		<p>Eat this burger!</p>
  		<textarea class="form-control" rows="2"></textarea>
		  <button type="submit" class="btn btn-danger" id="submit">Add Burger</button>
	  </form>	
  </div>
  <div class="col-md-4 col-md-4 col-md-4">
    
  </div>
</div>